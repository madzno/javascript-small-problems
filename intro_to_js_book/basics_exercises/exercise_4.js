console.log('5' + 10); // '510'
// When using a + operator where one operand is a String
// Javascript will perform explicit coercion of the other operand into
// a string no matter what the other operand is, and concatenate the
// results.
