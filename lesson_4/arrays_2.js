function rollCall(arr) {
  for (let index = 0; index < arr.length; index += 1) {
    console.log(arr[index]);
  }
}

rollCall(['Maddy', 'Ben', 'Tanya', 'Anthony']);
rollCall(['Maddy', 'Emily', 'Erin']);
