// let counter = 0;

// while (counter = 1) {
//   console.log(counter);
//   counter += 1;

//   if (counter > 2) {
//     break;
//   }
// }

// The conditional following while always evaluates to 1 which is a truthy value,
// thus for every iteration, 1 is returned which is a truthy value and thus the
// condition following while never becomes false.
