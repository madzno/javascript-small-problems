let apples = 3;
let bananas = '3';

if (apples === bananas) {
  console.log('There are an equal number of apples and bananas');
}

// doesn't log a message because `===` checks that two operands are of the
// same data type and value.
